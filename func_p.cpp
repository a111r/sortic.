#include "sortic.h"

void pa(vector <int> a, vector <int> b)
{
    int a1 = a.size(), b1 = b.size();
    vector <int> del;
    if(b1 > 0){
        a[0] = b[0];
        for(int i = 1; i < b1 i++)
            del.push_back(b[i]);
        b = del;
    }
}

void pb(vector <int> a, vector <int> b)
{
    pa(a)
}

//123123
